# 📝 AI Class Game - 노션(Notion) 통합 데이터 연동 가이드

`AI Class Game` 관리자 페이지(`admin.html`)에서 5개 핵심 데이터 항목(**학생 명단**, **퀴즈 결과**, **게시판 글**, **업로드 현황**, **전체 통계**)을 **노션 데이터베이스(Notion Database)**에 연동하는 방법 안내 문서입니다.

---

## 🎯 선택 가능한 5가지 노션 동기화 데이터 항목

관리자 페이지 노션 연동 모달에서 원하는 데이터를 선택하여 노션 API 전송 또는 CSV 다운로드가 가능합니다:

1. **👥 학생 계정 명단 (`students`)**: 학생 이름, 학번, 학교, 아이디, 점수, 캐릭터명, 가입일
2. **📝 퀴즈 응시 결과 (`quiz`)**: 학생이름, 학번, 퀴즈명(퀘스트), 응시횟수, 최고점수, 평균점수, 평균정답률(%), 마지막 응시시각
3. **📋 게시판 작성 글 (`board`)**: 글 제목/내용, 작성자 이름, 작성자 ID, 게시판 종류, 획득 점수(XP), 작성일시
4. **📁 학생 업로드 현황 (`uploads`)**: 파일명, 학생이름, 사용자 ID, 이미지 URL, 업로드 일시
5. **📊 전체 통계 & 설문 요약 (`stats`)**: 총 학생 수, 퀘스트 진행 학생 수, 총 퀴즈 시도 횟수, 평균 점수, 사전/사후 설문 응답자 수 등

---

## 🚀 방법 1: Notion API 자동 동기화 (권장)

### 1단계: Notion API Key (Integration Token) 발급
1. [Notion Integrations 관리 페이지](https://www.notion.so/my-integrations) 접속 후 로그인합니다.
2. **`+ 새 통합 만들기` (+ New Integration)** 클릭 ➔ 이름(예: `AI Class Sync`) 지정 후 저장
3. 생성된 **`내부 통합 시크릿 (Internal Integration Secret)`** 키(`secret_...`)를 복사합니다.

### 2단계: 노션 데이터베이스 생성 및 연동 추가
1. 노션(Notion)에서 새 페이지 생성 ➔ **`/표 - 전체 페이지`** 선택
2. 표 우측 상단 `...` 버튼 ➔ **`연동 추가` (Add connections)** ➔ 1단계 통합 키 허용
3. 선택한 항목에 맞춰 데이터베이스 속성(Column)을 설정합니다:

#### 📌 속성 세팅 매핑 가이드
- **퀴즈 결과 선택 시**: `학생이름`(제목), `학번`(텍스트), `퀴즈명`(선택), `응시횟수`(숫자), `최고점수`(숫자), `평균점수`(숫자), `평균정답률`(숫자), `마지막응시`(텍스트)
- **게시판 글 선택 시**: `제목`(제목), `작성자`(텍스트), `작성자ID`(텍스트), `게시판`(선택), `획득점수`(숫자), `작성일시`(텍스트)
- **업로드 현황 선택 시**: `파일명`(제목), `학생이름`(텍스트), `사용자ID`(텍스트), `이미지URL`(URL), `업로드일시`(텍스트)
- **전체 통계 선택 시**: `항목`(제목), `수치`(숫자), `상세설명`(텍스트)

### 3단계: Notion Database ID 입력 및 전송
1. 노션 웹 URL의 32자리 Database ID 복사
2. `admin.html`에서 **`📝 노션 연동`** 클릭 ➔ 전송할 **데이터 항목 선택** ➔ API Key & Database ID 입력 ➔ **`🚀 노션으로 데이터 일괄 전송`** 클릭!

---

## 📥 방법 2: 노션 전용 CSV 일괄 가져오기 (가장 쉬운 1초 방법)

1. `admin.html`에서 **`📝 노션 연동`** 클릭
2. **전송할 데이터 항목** 선택 (예: 퀴즈 결과, 게시판 글 등)
3. **`📥 노션 가져오기 전용 CSV 다운로드`** 클릭
4. 노션 좌측 하단 **`가져오기 (Import)`** ➔ **`CSV`** 선택 ➔ 1초 만에 데이터베이스 완성!
